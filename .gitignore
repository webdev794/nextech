# Local temporary output and package caches
/.tmp/

# backend/web_deploy/ (deploy zip, DB dump, demo videos, docs) IS committed as
# a backup; its large files go through Git LFS (see .gitattributes).
