# Used automatically by `npm run build`. Committed on purpose: no secrets here,
# only public URLs. The Stripe publishable key comes from GET /api/config at runtime.
VITE_BASE=/gdp/
VITE_API_URL=/gdp/api
